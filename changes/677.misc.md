Modularize `RenameProvider`

The rename provider now runs a set of modules, one per renamer, instead of a single
branching method, mirroring the hover, definitions, and document highlight providers'
structure.
