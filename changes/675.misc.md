Modularize `InlayHintProvider`

The inlay hint provider now runs a set of modules, one per concern (argument names, atom typing,
invocation typing), combining all of their results, rather than two ad-hoc supplier classes joined
by a single `Stream.concat`.
