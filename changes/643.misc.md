Ensure stable sorting when writing JSONL type databases.

Writing the same definitions twice now produces a byte-for-byte identical `types.jsonl`, so type
databases can be compared directly or committed as a baseline.
