Add `UnnamedProcedure`/`unnamed-procedure` check for `magik` files.

The new check reports an issue when a procedure (`_proc()`) has no label.

## Breaking changes

This new check
might result in new issues in your codebase.
