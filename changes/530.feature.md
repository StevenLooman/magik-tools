The `TypeDocCheck` now reports a `Missing return documentation.` issue when a
method or procedure contains a `_return` statement with a value, or an emit
(`>>`), but has no corresponding `@return` TypeDoc comment.
