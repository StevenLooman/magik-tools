Protect writing `types.jsonl` from being interrupted, use UTF8 encoding for JSON.

It might happen that the `types.jsonl` was not being fully written on language server
shutdown. This is now fixed.

Breaking changes:

The JSONL type databases were written/read using ISO8859-1 encoding. This is changed to UTF8.
You might have to re-dump your type databases.
