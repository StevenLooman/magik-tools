Refactor reporter wiring for `magik-lint` and `magik-typed-lint`.

Reporter creation is now more loosely coupled via shared reporter abstractions
in `magik-checks`, with centralized registry/context-based construction in the
CLI entry points. This keeps the linters aligned while reducing direct
instantiation logic in each module.
