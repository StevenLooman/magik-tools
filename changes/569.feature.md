Add support for `_slot(name)` references in type docs and type strings.

The `_slot(name)` references are used to indirectly get the type from a slot, which
must have been annontated using `@slot {type_string} name` on definition.
