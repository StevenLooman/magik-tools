Accept a conditional expression whose type responds to `should_be_boolean()`.

The `conditional-expression-is-false` check previously flagged any conditional
expression that did not evaluate to `sw:false`. Magik also accepts a value whose
type defines a `should_be_boolean()` method, using it to coerce the value to a
boolean, so such expressions are no longer reported.
