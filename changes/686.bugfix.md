Fix semantic tokenizing of `@generic`

A `## @generic` line received no semantic tokens at all. Its name is now tokenized as a type
parameter, and the tag accepts an optional type, as in `## @generic {sw:char16_vector} E`,
which is tokenized like the type of any other tag.
