Modularize hover, fixes where hover showed nothing

The hover provider now runs a set of modules, one per hovered construct, instead of a single
branching method. Additionally, some situations where no hover was provided but should have
been were fixed.
