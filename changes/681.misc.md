Deduplicate `FoldingRangeProvider`'s module.def/product.def logic and add tests

`FoldingRangeProvider`'s `ModuleDefFile` and `ProductDefFile` overloads shared byte-for-byte
identical bodies, now extracted into a single private helper. Also added the first
`FoldingRangeProviderTest`, covering all four `provideFoldingRanges` overloads.
