Modularize `DocumentHighlightProvider`

The document highlight provider now runs a set of modules, one per construct, instead of a single
branching method, mirroring the hover and definitions providers' structure.
