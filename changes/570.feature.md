When an _if-expression uses `>>` in one or more of its branches, it produces a value.
Discarding that value without consuming it is almost certainly a bug. Assign it, emit it, or return it.

Adding a new MagikCheck for this (UnusedIfResult) to cover this.
