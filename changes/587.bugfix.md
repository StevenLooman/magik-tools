Exclude non-test methods from MUnit test discovery.

Methods such as `tests()`, `test_aspects`, `test_aspects_keys`, and `test_result`
start with `test` but are part of the `sw:test_case` framework rather than actual
tests. They are no longer reported as runnable tests.
