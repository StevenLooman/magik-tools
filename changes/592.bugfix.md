Check `@return` type docs on `define_shared_constant`/`define_shared_variable` definitions.

`CallableReturnTypesMatchDoc` now also checks (top level) `define_shared_constant()` and
`define_shared_variable()` invocations.
