Modularize `TypeHierarchyProvider`

The type hierarchy provider now runs a set of modules, one per construct, instead of a single
branching method, mirroring the hover, definitions, and document highlight providers' structure.
