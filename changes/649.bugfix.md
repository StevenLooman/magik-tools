Write procedure definitions to `types.jsonl` with the `procedure` instruction.

Every procedure definition was written with the `method` instruction, resulting it reading it back
as a method definition named `dummy_method` to its type instead.
