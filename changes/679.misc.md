Modularize `SymbolProvider`

The symbol provider now runs a set of modules, one per construct, combining all of their results,
rather than five methods mutating a shared list, mirroring the inlay hint provider's structure.
