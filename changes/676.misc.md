Modularize `CodeActionProvider`

The code action provider now runs a set of modules, one per contributor (checks and the three
extract-refactoring providers), combining all of their results, rather than five ad-hoc
sub-providers joined by nested `Stream.concat` calls.
