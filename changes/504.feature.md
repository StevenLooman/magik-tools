Add SARIF output format to `magik-lint` and `magik-typed-lint`.

Both linters now accept a `--format sarif` option that emits a
[SARIF 2.1.0](https://docs.oasis-open.org/sarif/sarif/v2.1.0/sarif-v2.1.0.html)
document to stdout instead of the default line-per-issue format. The SARIF
output includes:

- A `rules` array with every enabled check (id, name, short description,
  default severity level, and tags).
- A `results` array with the rule id, severity level, message text, and a
  `physicalLocation` with a `%SRCROOT%`-relative URI and a precise
  start/end region for each reported issue.

This makes it straightforward to upload results to GitHub Code Scanning via
the `github/codeql-action/upload-sarif` action, which renders findings as
inline annotations on pull requests and tracks them in the Security tab.

The reporter classes (`Reporter`, `NullReporter`, `MessageFormatReporter`,
`SarifReporter`) are now consolidated in the `magik-checks` module and
shared by both linters; the previously duplicated `output` packages in
`magik-lint` and `magik-typed-lint` have been removed.
