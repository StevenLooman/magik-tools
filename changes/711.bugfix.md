Report the used `magik-lint.properties` in `--show-checks` output

The location of the configuration file was only visible through `--debug` logging, and the loggers
reporting it had been disabled for being noisy. `--show-checks` now starts with a `Configuration
file:` line naming the configuration in use.
